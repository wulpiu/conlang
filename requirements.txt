PyQt6>=6.6

# Опционально — нужны только для анализа текста на естественных языках
# (программа запускается и без них, просто соответствующий раздел
# TextAnalyzer будет недоступен для этого языка)
pymorphy3>=2.0     # морфология русского
spacy>=3.7         # английский (плюс: python -m spacy download en_core_web_sm)
stanza>=1.8        # баскский
#Для тестов
pytest>=8.0
ruff>=0.5
mypy>=1.10